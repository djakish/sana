fn main() {
    println!(
        "sana {} — object-storage-native search database",
        env!("CARGO_PKG_VERSION")
    );
    println!("stage 0: object store, manifest, and wal codecs are in place");
    println!("see docs/wiki/architecture.md and docs/PROGRESS.md");
}
